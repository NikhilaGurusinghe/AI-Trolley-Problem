{
  "name": "p5",
  "module": "src/sketch.ts",
  "type": "module",
  "scripts": {
    "watch": "bun build src/sketch.ts --outdir ./dist --target browser --watch",
    "build": "bun build src/sketch.ts --outdir ./dist --target browser --minify",
    "lint": "bunx @biomejs/biome lint src",
    "format": "bunx @biomejs/biome format src --write"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@types/p5": "^1.7.7",
    "p5": "^1.9.4",
    "ts-loader": "^9.5.7",
    "typescript": "^6.0.3"
  }
}
