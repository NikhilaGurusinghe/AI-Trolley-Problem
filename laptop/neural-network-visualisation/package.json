{
  "name": "p5",
  "module": "src/sketch.ts",
  "type": "module",
  "scripts": {
    "watch": "bun build src/sketch.ts --outdir ./dist --target browser --watch --banner '// Made with <3 by humans'",
    "build": "bun build src/sketch.ts --outdir ./dist --target browser --minify --splitting --banner '// Made with <3 by humans'",
    "lint": "bunx @biomejs/biome lint src",
    "format": "bunx @biomejs/biome format src --write"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^25.6.0"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@types/p5": "^1.7.7",
    "p5": "^2.2.3",
    "ts-loader": "^9.5.7",
    "typescript": "^6.0.3"
  }
}
